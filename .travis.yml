sudo: false
language: node_js
node_js:
  - 7
  - 8
cache:
  directories:
    - node_modules
branches:
  only:
    - master
notifications:
  email: false
before_install: if [[ `npm -v` != 7* ]]; then npm i -g npm@7; fi
install:
  - npm install

before_script:
  - npm prune
script:
  - npm run build